# Copy to .env and fill in. Never commit the real .env.

# Locks the owner profile on first boot. Ignored once a password exists.
OWNER_PASSWORD=

# Signs session cookies. Generate one with:
#   openssl rand -hex 32
SESSION_SECRET=
