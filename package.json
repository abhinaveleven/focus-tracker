{
    "name": "focus-tracker",
    "version": "1.0.0",
    "description": "A self-hosted focus tracker with PostgreSQL",
    "main": "server.js",
    "scripts": {
        "start": "node server.js",
        "db:migrate": "npx prisma migrate dev",
        "db:generate": "npx prisma generate"
    },
    "dependencies": {
        "@prisma/client": "^5.10.0",
        "express": "^4.18.2"
    },
    "devDependencies": {
        "prisma": "^5.10.0"
    }
}